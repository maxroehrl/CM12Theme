<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- radius of the corners of the material rounded rect background -->
    <dimen name="notification_material_rounded_rect_radius">0dp</dimen>

    <!-- radius of the corners of the material rounded rect background but negative-->
    <dimen name="notification_material_rounded_rect_radius_negative">0dp</dimen>

    <!-- radius of the corners of the notification dropshadow -->
    <dimen name="notification_material_shadow_rounded_rect_radius">0dp</dimen>

    <!-- The size of the application icon in the recents task view. -->
    <dimen name="recents_task_view_application_icon_size">60dp</dimen>

    <!-- The radius of the rounded corners on a task view. -->
    <dimen name="recents_task_view_rounded_corners_radius">0dp</dimen>

    <!-- The height of a task view bar. -->
    <dimen name="recents_task_bar_height">65dp</dimen>

    <!-- Size of the qs icons. -->
    <dimen name="qs_tile_icon_size">48dp</dimen>

    <!-- Padding between qs icon and label -->
    <dimen name="qs_tile_padding_below_icon">5dp</dimen>

    <!-- The side padding of the notifications-->
    <dimen name="notification_side_padding">0dp</dimen>

    <dimen name="qs_stroke_width">8dp</dimen>
    <dimen name="qs_stroke_width_negative">-8dp</dimen>
</resources>